<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.eunmi.board.mapper.BoardMapper">

	<!-- 게시판 리스트	- select -->
	<select id="list" resultType="com.eunmi.board.vo.BoardVO">
		SELECT no, title, writer, writeData, hit
		FROM (
			SELECT rownum rnum, no, title, writer, writeData, hit
			FROM (
				SELECT no, title, writer, writeData, hit
				FROM board
				ORDER BY no DESC
			)
		) 
		WHERE rnum between #{startRow} and #{endRow}
	</select>
	
	<!-- 전체 데이터 개수 가져오기 - select -->
	<select id="getTotalRow" resultType="java.lang.Long">
		SELECT count(*) 
		FROM board
	</select>
</mapper>